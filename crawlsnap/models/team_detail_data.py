# coding: utf-8

"""
    CrawlSnap API

    CrawlSnap is a data intelligence platform. It delivers structured, on-demand data through fast, typed HTTP APIs you can call from any language. This reference covers authentication, the response envelope, error handling, and the available CrawlSnap data products:    - **VectorSnap** — reputation, detections, categories, and relationships     for url / hash / ip / domain.   - **PulseSnap** — threat-intelligence pulse (and sandbox) enrichment for     url / hash / ip / domain.   - **SubdoSnap** — paginated subdomain enumeration for a domain.   - **SportSnap** — live football (soccer) data: live scores with     in-match events, fixtures with per-region broadcast channels, match     details (lineups, events, statistics, per-country coverage),     competitions (fixtures, standings, top scorers, TV rights), teams,     TV channel directories, football news, search, and player profiles.   - **SerpApi** — ranked Google search results (SERP) with real, already     unwrapped target URLs, plus related-search suggestions.  ## Authentication  Authenticate every request with your CrawlSnap API key, sent as a Bearer token in the `Authorization` header:      Authorization: Bearer sk-cs-...  Create and rotate keys from your dashboard. Treat the key like a password: it carries your full quota and must stay secret. Never embed it in client-side code or commit it to source control.  ## Response envelope  Every response — success or error — uses the same envelope:  ```json {   \"data\": { ... },          // payload on success, null on failure   \"is_success\": true,        // authoritative success flag   \"message\": \"Success\",     // human-readable summary   \"response_code\": 200       // mirrors the HTTP status code } ```  Always check `is_success` before reading `data`.  ## Status codes  HTTP status codes follow standard REST semantics; the body `response_code` mirrors the HTTP status.    - **200** — success, `data` populated.   - **400** — invalid input (malformed query or path parameter).   - **401** — missing or invalid API key.   - **402** — out of credits, or monthly quota exceeded.   - **403** — subscription is not active.   - **404** — no data found for the supplied identifier.   - **429** — daily request limit exceeded.   - **5xx** — server error, or the upstream enrichment service was     unavailable / timed out. 

    The version of the OpenAPI document: 1.0.0
    Contact: support@crawlsnap.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from crawlsnap.models.fixture_summary import FixtureSummary
from crawlsnap.models.squad_player import SquadPlayer
from crawlsnap.models.team import Team
from crawlsnap.models.team_competition import TeamCompetition
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class TeamDetailData(BaseModel):
    """
    TeamDetailData
    """ # noqa: E501
    team: Team
    squad: Optional[List[SquadPlayer]] = None
    competitions: Optional[List[TeamCompetition]] = None
    fixtures: Optional[List[FixtureSummary]] = None
    fixtures_next: Optional[StrictStr] = None
    fixtures_prev: Optional[StrictStr] = None
    __properties: ClassVar[List[str]] = ["team", "squad", "competitions", "fixtures", "fixtures_next", "fixtures_prev"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TeamDetailData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of team
        if self.team:
            _dict['team'] = self.team.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in squad (list)
        _items = []
        if self.squad:
            for _item_squad in self.squad:
                if _item_squad:
                    _items.append(_item_squad.to_dict())
            _dict['squad'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in competitions (list)
        _items = []
        if self.competitions:
            for _item_competitions in self.competitions:
                if _item_competitions:
                    _items.append(_item_competitions.to_dict())
            _dict['competitions'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in fixtures (list)
        _items = []
        if self.fixtures:
            for _item_fixtures in self.fixtures:
                if _item_fixtures:
                    _items.append(_item_fixtures.to_dict())
            _dict['fixtures'] = _items
        # set to None if fixtures_next (nullable) is None
        # and model_fields_set contains the field
        if self.fixtures_next is None and "fixtures_next" in self.model_fields_set:
            _dict['fixtures_next'] = None

        # set to None if fixtures_prev (nullable) is None
        # and model_fields_set contains the field
        if self.fixtures_prev is None and "fixtures_prev" in self.model_fields_set:
            _dict['fixtures_prev'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TeamDetailData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "team": Team.from_dict(obj["team"]) if obj.get("team") is not None else None,
            "squad": [SquadPlayer.from_dict(_item) for _item in obj["squad"]] if obj.get("squad") is not None else None,
            "competitions": [TeamCompetition.from_dict(_item) for _item in obj["competitions"]] if obj.get("competitions") is not None else None,
            "fixtures": [FixtureSummary.from_dict(_item) for _item in obj["fixtures"]] if obj.get("fixtures") is not None else None,
            "fixtures_next": obj.get("fixtures_next"),
            "fixtures_prev": obj.get("fixtures_prev")
        })
        return _obj


