# coding: utf-8

"""
    CrawlSnap API

    CrawlSnap is a data intelligence platform. It delivers structured, on-demand data through fast, typed HTTP APIs you can call from any language. This reference covers authentication, the response envelope, error handling, and the available CrawlSnap data products:    - **VectorSnap** — reputation, detections, categories, and relationships     for url / hash / ip / domain.   - **PulseSnap** — threat-intelligence pulse (and sandbox) enrichment for     url / hash / ip / domain.   - **SubdoSnap** — paginated subdomain enumeration for a domain.   - **SportSnap** — live football (soccer) TV listings: channel metadata     and broadcast schedules, match details with per-country coverage     (score, events, statistics, and lineups for finished matches),     country channel directories, and daily schedules.  ## Authentication  Authenticate every request with your CrawlSnap API key, sent as a Bearer token in the `Authorization` header:      Authorization: Bearer sk-cs-...  Create and rotate keys from your dashboard. Treat the key like a password: it carries your full quota and must stay secret. Never embed it in client-side code or commit it to source control.  ## Response envelope  Every response — success or error — uses the same envelope:  ```json {   \"data\": { ... },          // payload on success, null on failure   \"is_success\": true,        // authoritative success flag   \"message\": \"Success\",     // human-readable summary   \"response_code\": 200       // mirrors the HTTP status code } ```  Always check `is_success` before reading `data`.  ## Status codes  HTTP status codes follow standard REST semantics; the body `response_code` mirrors the HTTP status.    - **200** — success, `data` populated.   - **400** — invalid input (malformed query or path parameter).   - **401** — missing or invalid API key.   - **402** — out of credits, or monthly quota exceeded.   - **403** — subscription is not active.   - **404** — no data found for the supplied identifier.   - **429** — daily request limit exceeded.   - **5xx** — server error, or the upstream enrichment service was     unavailable / timed out. 

    The version of the OpenAPI document: 1.0.0
    Contact: support@crawlsnap.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from crawlsnap.models.competition_ref import CompetitionRef
from crawlsnap.models.country_broadcast import CountryBroadcast
from crawlsnap.models.lineups import Lineups
from crawlsnap.models.match_event import MatchEvent
from crawlsnap.models.match_stat import MatchStat
from crawlsnap.models.match_status import MatchStatus
from crawlsnap.models.score import Score
from crawlsnap.models.team_ref import TeamRef
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class MatchData(BaseModel):
    """
    MatchData
    """ # noqa: E501
    id: StrictInt
    status: MatchStatus
    competition: CompetitionRef
    round: Optional[StrictStr] = None
    home_team: TeamRef
    away_team: TeamRef
    is_placeholder: StrictBool = Field(description="True when at least one side is an undecided knockout-bracket slot (the source renders placeholders like \"W93\" or \"L101\" until the pairing is known).")
    kickoff_utc: Optional[datetime] = Field(default=None, description="Kickoff normalized to UTC; null if the time could not be parsed.")
    kickoff_local: Optional[StrictStr] = Field(default=None, description="Kickoff date+time in the source's rendering zone, cleaned of live/status markers (e.g. \"Jul 5, 2026 16:00\"); null if the time could not be parsed.")
    kickoff_raw: Optional[StrictStr] = Field(default=None, description="Raw kickoff header exactly as rendered by the source — debug/diagnostic aid.")
    venue: Optional[StrictStr] = None
    score: Optional[Score] = None
    events: Optional[List[MatchEvent]] = Field(default=None, description="Empty for scheduled matches.")
    stats: Optional[List[MatchStat]] = Field(default=None, description="Empty for scheduled matches.")
    lineups: Optional[Lineups] = None
    broadcasts: List[CountryBroadcast] = Field(description="Per-country broadcast coverage from the source's international coverage table.")
    highlights_url: Optional[StrictStr] = Field(default=None, description="Official highlights link for finished matches, when present.")
    updated_at: datetime
    __properties: ClassVar[List[str]] = ["id", "status", "competition", "round", "home_team", "away_team", "is_placeholder", "kickoff_utc", "kickoff_local", "kickoff_raw", "venue", "score", "events", "stats", "lineups", "broadcasts", "highlights_url", "updated_at"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MatchData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of competition
        if self.competition:
            _dict['competition'] = self.competition.to_dict()
        # override the default output from pydantic by calling `to_dict()` of home_team
        if self.home_team:
            _dict['home_team'] = self.home_team.to_dict()
        # override the default output from pydantic by calling `to_dict()` of away_team
        if self.away_team:
            _dict['away_team'] = self.away_team.to_dict()
        # override the default output from pydantic by calling `to_dict()` of score
        if self.score:
            _dict['score'] = self.score.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in events (list)
        _items = []
        if self.events:
            for _item_events in self.events:
                if _item_events:
                    _items.append(_item_events.to_dict())
            _dict['events'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in stats (list)
        _items = []
        if self.stats:
            for _item_stats in self.stats:
                if _item_stats:
                    _items.append(_item_stats.to_dict())
            _dict['stats'] = _items
        # override the default output from pydantic by calling `to_dict()` of lineups
        if self.lineups:
            _dict['lineups'] = self.lineups.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in broadcasts (list)
        _items = []
        if self.broadcasts:
            for _item_broadcasts in self.broadcasts:
                if _item_broadcasts:
                    _items.append(_item_broadcasts.to_dict())
            _dict['broadcasts'] = _items
        # set to None if round (nullable) is None
        # and model_fields_set contains the field
        if self.round is None and "round" in self.model_fields_set:
            _dict['round'] = None

        # set to None if kickoff_utc (nullable) is None
        # and model_fields_set contains the field
        if self.kickoff_utc is None and "kickoff_utc" in self.model_fields_set:
            _dict['kickoff_utc'] = None

        # set to None if kickoff_local (nullable) is None
        # and model_fields_set contains the field
        if self.kickoff_local is None and "kickoff_local" in self.model_fields_set:
            _dict['kickoff_local'] = None

        # set to None if kickoff_raw (nullable) is None
        # and model_fields_set contains the field
        if self.kickoff_raw is None and "kickoff_raw" in self.model_fields_set:
            _dict['kickoff_raw'] = None

        # set to None if venue (nullable) is None
        # and model_fields_set contains the field
        if self.venue is None and "venue" in self.model_fields_set:
            _dict['venue'] = None

        # set to None if score (nullable) is None
        # and model_fields_set contains the field
        if self.score is None and "score" in self.model_fields_set:
            _dict['score'] = None

        # set to None if lineups (nullable) is None
        # and model_fields_set contains the field
        if self.lineups is None and "lineups" in self.model_fields_set:
            _dict['lineups'] = None

        # set to None if highlights_url (nullable) is None
        # and model_fields_set contains the field
        if self.highlights_url is None and "highlights_url" in self.model_fields_set:
            _dict['highlights_url'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MatchData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "status": obj.get("status"),
            "competition": CompetitionRef.from_dict(obj["competition"]) if obj.get("competition") is not None else None,
            "round": obj.get("round"),
            "home_team": TeamRef.from_dict(obj["home_team"]) if obj.get("home_team") is not None else None,
            "away_team": TeamRef.from_dict(obj["away_team"]) if obj.get("away_team") is not None else None,
            "is_placeholder": obj.get("is_placeholder"),
            "kickoff_utc": obj.get("kickoff_utc"),
            "kickoff_local": obj.get("kickoff_local"),
            "kickoff_raw": obj.get("kickoff_raw"),
            "venue": obj.get("venue"),
            "score": Score.from_dict(obj["score"]) if obj.get("score") is not None else None,
            "events": [MatchEvent.from_dict(_item) for _item in obj["events"]] if obj.get("events") is not None else None,
            "stats": [MatchStat.from_dict(_item) for _item in obj["stats"]] if obj.get("stats") is not None else None,
            "lineups": Lineups.from_dict(obj["lineups"]) if obj.get("lineups") is not None else None,
            "broadcasts": [CountryBroadcast.from_dict(_item) for _item in obj["broadcasts"]] if obj.get("broadcasts") is not None else None,
            "highlights_url": obj.get("highlights_url"),
            "updated_at": obj.get("updated_at")
        })
        return _obj


