# crawlsnap
The CrawlSnap API provides on-demand threat-intelligence enrichment for
indicators of compromise (IoCs). Submit a URL, file hash, IPv4 address, or
domain and receive structured enrichment from multiple upstream sources:

  - **vector-scan** — VirusTotal-derived reputation, detections, and
    relationships for url / hash / ip / domain.
  - **pulse-snap** — AlienVault OTX pulse (and sandbox) enrichment for
    url / hash / ip / domain.
  - **subdo-snap** — paginated subdomain enumeration for a domain.

## Authentication

Authenticate every request with your CrawlSnap API key, sent as a Bearer
token in the `Authorization` header:

    Authorization: Bearer sk-cs-...

Create and rotate keys from your dashboard. Treat the key like a password:
it carries your full quota and must stay secret. Never embed it in
client-side code or commit it to source control.

## Response envelope

Every response — success or error — uses the same envelope:

```json
{
  \"data\": { ... },          // payload on success, null on failure
  \"is_success\": true,        // authoritative success flag
  \"message\": \"Success\",     // human-readable summary
  \"response_code\": 200       // mirrors the HTTP status code
}
```

Always check `is_success` before reading `data`.

## Status codes

HTTP status codes follow standard REST semantics; the body `response_code`
mirrors the HTTP status.

  - **200** — success, `data` populated.
  - **400** — invalid input (malformed URL / hash / IP / domain).
  - **401** — missing or invalid API key.
  - **402** — out of credits, or monthly quota exceeded.
  - **403** — subscription is not active.
  - **404** — no IoC data found for the supplied indicator.
  - **429** — daily request limit exceeded.
  - **5xx** — server error, or the upstream enrichment service was
    unavailable / timed out.


This Python package is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0.0
- Package version: 0.1.0
- Generator version: 7.22.0
- Build package: org.openapitools.codegen.languages.PythonClientCodegen
For more information, please visit [https://crawlsnap.com](https://crawlsnap.com)

## Requirements.

Python 3.10+

## Installation & Usage
### pip install

If the python package is hosted on a repository, you can install directly using:

```sh
pip install git+https://github.com/GIT_USER_ID/GIT_REPO_ID.git
```
(you may need to run `pip` with root permission: `sudo pip install git+https://github.com/GIT_USER_ID/GIT_REPO_ID.git`)

Then import the package:
```python
import crawlsnap
```

### Setuptools

Install via [Setuptools](http://pypi.python.org/pypi/setuptools).

```sh
python setup.py install --user
```
(or `sudo python setup.py install` to install the package for all users)

Then import the package:
```python
import crawlsnap
```

### Tests

Execute `pytest` to run the tests.

## Getting Started

Please follow the [installation procedure](#installation--usage) and then run the following:

```python

import crawlsnap
from crawlsnap.rest import ApiException
from pprint import pprint

# Defining the host is optional and defaults to https://api.crawlsnap.com
# See configuration.py for a list of all supported configuration parameters.
configuration = crawlsnap.Configuration(
    host = "https://api.crawlsnap.com"
)

# The client must configure the authentication and authorization parameters
# in accordance with the API server security policy.
# Examples for each auth method are provided below, use the example that
# satisfies your auth use case.

# Configure Bearer authorization: bearerAuth
configuration = crawlsnap.Configuration(
    access_token = os.environ["BEARER_TOKEN"]
)


# Enter a context with an instance of the API client
with crawlsnap.ApiClient(configuration) as api_client:
    # Create an instance of the API class
    api_instance = crawlsnap.PulseSnapApi(api_client)
    query = 'google.com' # str | The domain to enrich.

    try:
        # AlienVault OTX pulse enrichment for a domain.
        api_response = api_instance.pulse_snap_domain(query)
        print("The response of PulseSnapApi->pulse_snap_domain:\n")
        pprint(api_response)
    except ApiException as e:
        print("Exception when calling PulseSnapApi->pulse_snap_domain: %s\n" % e)

```

## Documentation for API Endpoints

All URIs are relative to *https://api.crawlsnap.com*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*PulseSnapApi* | [**pulse_snap_domain**](docs/PulseSnapApi.md#pulse_snap_domain) | **GET** /v1/pulse-snap/scan/domain | AlienVault OTX pulse enrichment for a domain.
*PulseSnapApi* | [**pulse_snap_hash**](docs/PulseSnapApi.md#pulse_snap_hash) | **GET** /v1/pulse-snap/scan/hash | AlienVault OTX pulse + sandbox enrichment for a file hash.
*PulseSnapApi* | [**pulse_snap_ip**](docs/PulseSnapApi.md#pulse_snap_ip) | **GET** /v1/pulse-snap/scan/ip | AlienVault OTX pulse enrichment for an IPv4 address.
*PulseSnapApi* | [**pulse_snap_url**](docs/PulseSnapApi.md#pulse_snap_url) | **GET** /v1/pulse-snap/scan/url | AlienVault OTX pulse enrichment for a URL.
*SubdoSnapApi* | [**subdo_snap_scan**](docs/SubdoSnapApi.md#subdo_snap_scan) | **GET** /v1/subdo-snap/scan | Paginated subdomain enumeration for a domain.
*VectorScanApi* | [**vector_scan_domain**](docs/VectorScanApi.md#vector_scan_domain) | **GET** /v1/ioc/search/domain | VirusTotal IoC enrichment for a domain.
*VectorScanApi* | [**vector_scan_hash**](docs/VectorScanApi.md#vector_scan_hash) | **GET** /v1/ioc/search/hash | VirusTotal IoC enrichment for a file hash.
*VectorScanApi* | [**vector_scan_ip**](docs/VectorScanApi.md#vector_scan_ip) | **GET** /v1/ioc/search/ip | VirusTotal IoC enrichment for an IPv4 address.
*VectorScanApi* | [**vector_scan_url**](docs/VectorScanApi.md#vector_scan_url) | **GET** /v1/ioc/search/url | VirusTotal IoC enrichment for a URL.


## Documentation For Models

 - [BaseResponse](docs/BaseResponse.md)
 - [ErrorResponse](docs/ErrorResponse.md)
 - [IocDomainScanData](docs/IocDomainScanData.md)
 - [IocHashScanData](docs/IocHashScanData.md)
 - [IocIpScanData](docs/IocIpScanData.md)
 - [IocUrlScanData](docs/IocUrlScanData.md)
 - [PulseDomainScanData](docs/PulseDomainScanData.md)
 - [PulseHashScanData](docs/PulseHashScanData.md)
 - [PulseIpScanData](docs/PulseIpScanData.md)
 - [PulseSnapDomainResponse](docs/PulseSnapDomainResponse.md)
 - [PulseSnapHashResponse](docs/PulseSnapHashResponse.md)
 - [PulseSnapIpResponse](docs/PulseSnapIpResponse.md)
 - [PulseSnapUrlResponse](docs/PulseSnapUrlResponse.md)
 - [PulseUrlScanData](docs/PulseUrlScanData.md)
 - [SubdoSnapScanData](docs/SubdoSnapScanData.md)
 - [SubdoSnapScanResponse](docs/SubdoSnapScanResponse.md)
 - [VectorScanDomainResponse](docs/VectorScanDomainResponse.md)
 - [VectorScanHashResponse](docs/VectorScanHashResponse.md)
 - [VectorScanIpResponse](docs/VectorScanIpResponse.md)
 - [VectorScanUrlResponse](docs/VectorScanUrlResponse.md)


<a id="documentation-for-authorization"></a>
## Documentation For Authorization


Authentication schemes defined for the API:
<a id="bearerAuth"></a>
### bearerAuth

- **Type**: Bearer authentication


## Author

support@crawlsnap.com


